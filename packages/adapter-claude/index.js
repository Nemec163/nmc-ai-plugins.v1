'use strict';

function createClaudeAdapter() {
  throw new Error(
    'adapter-claude is a MemoryOS.v1 scaffold only. Implement the Claude runtime contract before using it.'
  );
}

module.exports = {
  createClaudeAdapter,
};
